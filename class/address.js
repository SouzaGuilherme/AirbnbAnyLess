class address{
  constructor(logradouro, number, complement, district, zipcode){
    this.logradouro = logradouro;
    this.number = number;
    this.complement = complement;
    this.district = district;
    this.zipcode = zipcode;
  };

  get address(){};
  set address(newAddress){};
}

